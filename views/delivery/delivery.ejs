<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <style>
      body {
        width: 100vw;
        margin: 0;
        padding: 0;
      }
      main {
        display: flex;
      }
      nav {
        flex: 1;
        height: 7.5vh;
        background-color: #ff6400;
        color: #ffffff;
        position: sticky;
        top: 0;
      }
      div {
      }
      .sidebar {
        background-color: #ff660087;
        color: #ffff;
        display: flex;
        flex-direction: column;
        justify-content: stretch;
        /* align-items: center; */
        flex: 1;
        position: sticky;
        top: 7.5vh; /* same as nav's height */
        height: 92.5vh;
        padding: 10px 10px 0 10px;
      }

      a {
        text-decoration: none;
        color: inherit;
        padding: 0 10%;
        margin: 1% 0;
        font-weight: bold;
        font-size: large;
        display: flex;
        justify-content: space-evenly;
        align-items: center;
        border: 1px solid #ffd7bc;
        border-radius: 15px;
      }

      a:hover {
        background-color: #ffd7bc;
        border-radius: 15px;
        color: #ff6400;
      }
      a.active {
        background-color: #ffd7bc;
        border-radius: 30px;
        color: #ff6400;
      }

      .main-wrapper {
        flex: 4;
        overflow-y: scroll;
        scrollbar-color: none;
        padding: 10px;
        color: #ff6400;
        text-align: justify;
      }

      img {
        height: 40px;
        width: 40px;
      }
      li {
        padding: 10px;
        margin: 0, 15px;
      }
      h2 {
        padding: 0;
      }
      .boc {
        background-color: #ffd7bc;
        border: #ff6400;
        border-radius: 10px;
        box-shadow: 5px px 5px #ff6400;
        padding: 3%;
      }
    </style>
  </head>
  <body>
    <nav>
      <ul type="none" style="display: flex; justify-content: end">
        <li><a href="/">Home</a></li>
        <li><a href="/delivery/login">Login</a></li>
        <li><a href="/delivery/register">Register</a></li>
      </ul>
    </nav>
    <main>
      <div class="sidebar">
        <h2>Hello <%= user.name %></h2>
        <a href="/delivery"
          ><img src="open-order.png" alt="Open Order" />
          <p>Open Orders</p></a
        >
        <a class="active" href="delivery/ongoing-delivery">
          <img src="motorbike.png" alt="Ongoing Delivery" />
          <p>Ongoing Delivery</p></a
        >

        <a href="my-deliveries"
          ><img src="delivery.png" alt="My Deliveries" />
          <p>My Deliveries</p></a
        >
      </div>
      <div class="main-wrapper">
        <% if (messages.error) { %>
        <div class="error"><%= messages.error %></div>
        <% } %> <% if (messages.success) { %>
        <div class="success"><%= messages.success %></div>
        <% } %> <% if (order) { %>

        <div class="boc">
          <ul type="" none>
            <li><h3>Customer Name: <%= order.custName %></h3></li>
            <li><h3>Delivery Address: <%= order.deliveryAddr %></h3></li>
            <li>
              Order Date and Time: <%= new
              Date(order.orderTime).toLocaleString() %>
            </li>
          </ul>

          <h5>Done with your delivery ? Submit delivery Image here:</h5>
          <form
            action="/delivery/open-deliveries/deliver/<%= order.id %>"
            method="post"
            enctype="multipart/form-data"
          >
            <input type="file" id="myFile" name="proof" />
            <input type="submit" />
          </form>
          <% } else { %>
          <h2>You do not have any ongoing order !</h2>
          <% } %>
        </div>
      </div>
    </main>
    <footer></footer>
  </body>
</html>
